"""
アプリ横断で使う部品を置く層。

ここに入れるのは「特定のサービスに属さないが複数のアプリが必要とするもの」に限る。
業務ロジックは各アプリに置く。

Django アプリではない（INSTALLED_APPS には登録しない）。モデルもマイグレーションも
持たない純粋なパッケージとして扱う。

置いてある経緯:
    以前は no_cache_no_index を app/home に、is_superuser を accounts に置き、
    他アプリがそこから import していた。ハブ画面である home と、認証を担う accounts が
    共通部品の供給元を兼ねてしまい、依存の向きが実態と合わなくなっていたため分離した。
"""
